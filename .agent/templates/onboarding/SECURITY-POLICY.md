# {{name}} — Security Policy

> Generated by Devran AI Kit Onboarding Engine

## Overview

**Project:** {{name}}
**Platforms:** {{platforms}}

## Authentication

<!-- IF:hasAuth -->
- **Methods:** {{auth.method}}
- **Roles:** {{auth.roles}}
<!-- ENDIF:hasAuth -->

### Password Policy

- Minimum 12 characters
- Must include: uppercase, lowercase, number, special character
- Bcrypt hashing with cost factor 12+
- No password reuse (last 5)

### Session Management

- Token expiry: 15 minutes (access), 7 days (refresh)
- Secure, HttpOnly, SameSite cookies
- Session invalidation on password change

## Authorization

- Role-based access control (RBAC)
- Principle of least privilege
- Authorization checked at every endpoint

## Input Validation

- Validate ALL user input at system boundaries
- Use schema validation (e.g., Zod, Joi)
- Parameterized queries for ALL database operations
- Sanitize HTML output to prevent XSS

## Data Protection

- HTTPS everywhere (TLS 1.3)
- Encryption at rest for sensitive data
- PII handling per compliance requirements

<!-- IF:hasCompliance -->
### Compliance

Requirements: {{auth.compliance}}
<!-- ENDIF:hasCompliance -->

## Secret Management

- ALL secrets via environment variables
- Never commit secrets to version control
- Rotate secrets on schedule (90 days)
- Use secret manager in production (e.g., AWS Secrets Manager, Vault)

## Dependency Security

- Automated dependency scanning (npm audit, Snyk)
- No known critical vulnerabilities at deploy time
- Pin dependency versions

## Incident Response

1. Detect → 2. Contain → 3. Investigate → 4. Remediate → 5. Post-mortem

Refer to ARCHITECTURE.md for security architecture decisions.

## Next Steps

- [ ] Configure authentication system
- [ ] Set up dependency scanning in CI
- [ ] Create incident response runbook
