# {{name}} — API Specification

> Generated by Devran AI Kit Onboarding Engine

## Overview

**Project:** {{name}}
**API Style:** REST
**Base URL:** `/api/v1`

## Authentication

<!-- IF:hasAuth -->
- **Method:** {{auth.method}}
- **Roles:** {{auth.roles}}
<!-- ENDIF:hasAuth -->

All endpoints require authentication unless marked as `public`.

## Endpoints

### Health

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| GET | `/health` | public | Health check |
| GET | `/version` | public | API version |

<!-- IF:hasAuth -->
### Authentication

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| POST | `/auth/register` | public | Create account |
| POST | `/auth/login` | public | Authenticate |
| POST | `/auth/logout` | required | End session |
| POST | `/auth/refresh` | required | Refresh token |
<!-- ENDIF:hasAuth -->

### Core Resources

*Define endpoints from PRD.md core features.*

| Method | Path | Auth | Description |
|--------|------|------|-------------|
| *TBD* | *TBD* | *TBD* | *TBD* |

## Request/Response Format

### Success Response

```json
{
  "success": true,
  "data": {},
  "meta": { "total": 0, "page": 1, "limit": 20 }
}
```

### Error Response

```json
{
  "success": false,
  "error": { "code": "VALIDATION_ERROR", "message": "Description", "details": [] }
}
```

## Rate Limiting

- Default: 100 requests/minute per IP
- Authenticated: 1000 requests/minute per user
- Headers: `X-RateLimit-Limit`, `X-RateLimit-Remaining`, `X-RateLimit-Reset`

## Pagination

- Query params: `?page=1&limit=20`
- Max limit: 100
- Default limit: 20

## Versioning

- URL-based versioning: `/api/v1/`, `/api/v2/`
- Backward compatibility maintained for 6 months after deprecation

Refer to ARCHITECTURE.md for system context and SECURITY-POLICY.md for security requirements.

## Next Steps

- [ ] Define core resource endpoints from PRD features
- [ ] Create OpenAPI/Swagger specification
- [ ] Set up API documentation generation
